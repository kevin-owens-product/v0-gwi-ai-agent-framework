/**
 * Shared Components Barrel Export
 *
 * Centralized exports for all shared components used across both
 * admin and dashboard portals.
 *
 * @module components/shared
 */

// Collaboration
export { CommentsPanel } from "./comments-panel"

// Organization
export { FolderOrganization } from "./folder-organization"

// History & Versioning
export { VersionHistory } from "./version-history"

// Workflow
export { WorkflowStatus } from "./workflow-status"
